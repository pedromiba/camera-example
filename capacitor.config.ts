import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.camera.example',
  appName: 'camera-example',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
